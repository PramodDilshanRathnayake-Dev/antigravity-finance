# Antigravity Trading System - Environment Variables
# Copy this to .env and fill in the values

# Core AI Logic
OPENAI_API_KEY=sk-your-openai-key-here

# Database (Used by docker-compose)
POSTGRES_USER=antigravity
POSTGRES_PASSWORD=password
POSTGRES_DB=trading_engine

# Spring Boot overrides (Optional if using docker-compose defaults)
# SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/trading_engine
# SPRING_KAFKA_BOOTSTRAP_SERVERS=localhost:9092
